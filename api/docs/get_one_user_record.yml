user can get all records for a particular user (redflag & intervention)
---
tags:
  - Get user records (normal user route and admin)
parameters:
  - name: Authorization
    in: header
    required: true
    type: string
    description: Authorization header required for user authentication
  - name: user_id
    in: path
    description: This route is used by a user to retrieve already created records for a particular user
responses:
  200:
    description: 
security:
  - JWT:
    description: Pass in jwt token i.e Bearer <jwt>
    type: apiKey
    scheme: bearer
    name: Authorization
    in: header
    template: "Bearer {apiKey}"
    bearerFormat: JWT