user can create a recors(redflag or intervention)
---
tags:
  - Post records (normal user route)
parameters:
  - name: Authorization
    in: header
    required: true
    type: string
    description: Authorization header required for user authentication
  - name: body
    in: body
    required: true
    type: string
    description: This route is used by a user to post a new record
responses:
  201:
    description: Successfully posted a new record
  
security:
  - JWT:
    description: Pass in jwt token i.e Bearer <jwt>
    type: apiKey
    scheme: bearer
    name: Authorization
    in: header
    template: "Bearer {apiKey}"
    bearerFormat: JWT