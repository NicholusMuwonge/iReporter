
language: python

python:  
  - "3.7.1"

 
before install:
  - pip install -r requirements.txt
  - pip install pytest pytest-cov
  - pip install coveralls
  
  
 
  
script:   
  - py.test  

after_success:
  - coveralls
