
language: python

python:  
  - "3.5.1"

 
install:
  - "pip install -r requirements.txt"
  - "pip install pytest pytest-cov"
  - "pip install coveralls"
  
  
 
  
script:   
  - py.test  test.py

after_success:
  - coveralls
